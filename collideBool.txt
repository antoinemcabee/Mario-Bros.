//Main
/*bool collision(Player mario, Platform block);
    {
        if(mario.getY > block.getY + block.getH){
            return 0;}

        else if (mario.getX >block.getX+block.getH){
            return 0;}

        else if (mario.getY +mario.getH < block.getY ||mario.getY+mario.getH==block.getY){
            return 0;
        }

        else if (mario.getX + mario.getW <block.getX || mario.getX+mario.getW == block.getX){
            return 0;
        }
        else{
            return 1;
        }
    }*/

//Platform
 int getX(){
        return posX;
    };
    int getY(){
        return posY;
    };
    int getH(){
        return height;
    };
    int getW(){
        return width;
    };


//Player
int getY(){
        return posY;
    };
    int getX(){
        return posX;
    };
    int getH(){
        return height;
    };
    int getW(){
        return width;
    };